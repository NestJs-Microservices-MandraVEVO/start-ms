apiVersion: v1
kind: Service
metadata:
  labels:
    app: mongo-db-service
  name: mongo-db-service
spec:
  ports:
  - name: "27017"
    port: 27017
    protocol: TCP
    targetPort: 27017
  selector:
    app: mongo-db
  type: ClusterIP
status:
  loadBalancer: {}
